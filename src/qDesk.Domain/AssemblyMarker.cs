namespace qDesk.Domain;

/// <summary>
/// Empty anchor type used by reflection-based tooling (currently the architecture tests) to obtain a
/// reference to this assembly. A dedicated marker means that tooling does not break when a real
/// domain type is renamed or moved.
/// </summary>
public sealed class AssemblyMarker;
